# Ar nosurge 한국어 번역 작업 규칙

이 저장소에서 번역·검수·축약·빌드 작업을 할 때는 아래 규칙을 우선한다. 다른 게임 프로젝트의 공통 규칙과 충돌하면 **이 저장소의 기존 규칙·도구·용어집을 우선**한다.

## 권위본과 수정 위치

- 번역 편집 권위본은 `translations/` 아래 파일이다. `atmosphere/`는 생성 결과물이므로 직접 수정하지 않는다.
- 일본어 원문은 `originalText/`를 기준으로 대조하며 원문 데이터 자체를 번역 작업 때문에 수정하지 않는다.
- 고유명사·세계관 용어는 `tools/translate_main_japanese.py`와 `tools/translate_saves_with_ollama.py`의 `GLOSSARY`, 그리고 기존 확정 번역을 우선한다. 임의로 다른 표기를 만들지 않는다.
- `カソード`의 한국어 표기는 **`캐소드`**로 고정한다. `카소드`를 사용하지 않는다.
- 고유명사·세계관 용어의 음역은 사용자가 지정한 나무위키 `Ar nosurge` 및 관련 문서 표기를 우선한다. 현재 확인·고정한 핵심 표기는 `フェリオン→펠리온`, `タットリア→타토리아`, `クオンターヴ→퀀타브`, `シェルノトロン→셰르노트론`, `天統姫→텐토우키`, `天領沙羅→텐료사라`, `インターディメンド→인터디멘드`, `星詠台→성영대`, `アルノサージュ管→아르노사쥬관`, `レナルル→레나루루`, `ソレイル→소레일`, `ヒュムノス→휴므노스`, `ヒュムネス→휴므네스`, `ヒュムネスフィア→휴므네스피어`, `万寿沙羅→만쥬사라`, `イグジット→이그지트`, `刻神楽/刻神楽→토키카구라`, `シェルン→셰룬`, `シェルノサージュ→셰르노사쥬`, `ジリリウム→질리리움`이다. `シャール達` 및 같은 뜻의 `シャールたち`는 `샤르 일행`으로 옮기지 않고 **`샤르들`**로 번역하며 조사형은 `샤르들이/샤르들의/샤르들에게`처럼 자연스럽게 붙인다. 나무위키에서 확인되지 않은 세부 아이템명·기술명은 번역 빈도만으로 임의 확정하지 않는다.

- 세계관 원문·읽기 참고 자료로 [E.P.C.S.R. 용어집](https://exa7d.cloudfree.jp/innna/terminology)도 대조한다. 이미 확정한 한국어 표기를 이 자료만으로 임의 변경하지 않는다. `天領割符`는 `천령증표`, 일반어 `割符`는 `증표`로 구분한다.

## 말투와 호칭

- 화자와 상대의 관계에 따른 반말/존댓말, 격식 수준, 캐릭터 말투를 원문과 기존 번역에 맞춰 유지한다.
- 원문에 없는 호칭을 한국어에 임의로 추가하지 않는다.
- 기존 프로젝트 호칭 규칙을 따른다: `さん→씨`, `ちゃん→쨩`, `君/くん→군`, `様/さま→님`, `先輩→선배`, `先生→선생님`.
- `씨/쨩/군/님` 같은 이름 뒤 접미 호칭은 이 프로젝트의 기존 `fix_honorifics.py` 규칙대로 이름에 붙여 쓴다. 다른 프로젝트의 띄어쓰기 규칙을 가져오지 않는다.

## 문장부호와 늘임 표시

- 원문이 사용한 전각 문장부호는 기존 `restore_fullwidth_punctuation.py` 규칙을 우선한다. 특히 원문의 `～`는 번역에서도 `～` 종류를 유지하며 ASCII `~`로 바꾸지 않는다. 다만 한국어 표현상 자연스럽다면 기호 위치는 원문과 동일하게 고정하지 않고 옮길 수 있다. 예: `う～ん` → `으음～`.
- 원문이 전각/ASCII 형태를 한 단위 안에서 섞어 쓴 경우에는 임의로 전역 정규화하지 않는다.
- 발화·감탄 등에서 늘임 표시로 쓰인 원문의 `ー`는 번역에서도 `ー`로 유지하며 `～` 또는 `~`로 바꾸지 않는다.
- 단, 고유명사·일반 어휘 내부의 가타카나 장음부호는 기존 용어집의 한국어 독음이 우선한다. 예: `シャール→샤르`처럼 이미 확정된 표기를 `샤ー르`로 바꾸지 않는다.
- 원문의 `「」`, `『』` 등 프로젝트가 보존하는 기호는 임의의 따옴표로 치환하지 않는다.

## 의미와 형식 보존

- 원문의 주체, 부정, 조건, 시제, 핵심 정보와 뉘앙스를 생략하거나 반대로 바꾸지 않는다.
- `<CR>`, `<IMxx>`, `<RG>`, 색상 코드, printf 형식 등 제어 토큰은 원문 기준 철자·개수·순서·필요한 위치를 유지한다.
- EBM의 레코드 구조·메타데이터·NUL 종료 형식, XML의 번역 대상 외 속성은 변경하지 않는다.
- `main` 실행 파일 문자열은 고정 슬롯이므로 `capacity_bytes`를 절대 초과하지 않는다. 초과 시 기존 프로젝트 절차대로 공백/조사/어미를 줄이고 짧은 동의어를 쓰되 핵심 의미와 고유명사를 보존한다.

## 파일별 레이아웃 상한 (범용 규칙)

- 번역을 시작하기 전에 **대상 파일의 원문 텍스트 전체를 먼저 스캔**한다.
- 그 파일에서 실제로 사용된 값을 기준으로 `한 줄 최대 표시 길이`와 `한 텍스트의 최대 행 수`를 각각 계산한다. 기준은 특정 한 문장만 임의로 고르는 것이 아니라, 파일 전체에서 관측되는 최대값이다.
- 해당 파일의 번역은 계산된 `한 줄 최대 표시 길이`와 `최대 행 수`를 넘지 않게 작성·재배치한다. 원문보다 불필요하게 더 긴 줄이나 더 많은 행을 만들지 않는다.
- 표시 길이는 단순 UTF-8 바이트 수가 아니라 **게임 화면에서 차지하는 표시 단위**를 기준으로 센다. 공백·문장부호·전각문자·제어코드의 폭 계산은 해당 프로젝트의 기존 레이아웃 도구/규칙을 따른다.
- 엔진이나 화면별로 이미 확인된 하드 제한이 파일에서 관측한 값보다 더 엄격하면 **더 작은 쪽을 최종 상한**으로 사용한다. 현재 실게임 기준 확정값은 **이벤트 EBM 24표시칸×최대 3줄(72표시칸)**, **필드 `fm_talk_data` 22표시칸×최대 3줄(66표시칸)**이다. 그 외 기존 Saves 대화 계열은 별도 확인 전까지 기존 20표시칸×최대 3줄을 유지한다.
- `<CR>` 등 제어코드 처리 규칙은 `status.md`와 기존 빌드 도구를 따르며, 길이를 맞추기 위해 원문의 핵심 의미·호칭·고유명사·조건을 삭제하지 않는다.

## 확정 UI 레이아웃 규칙

아래 값은 실게임 화면에서 확인하며 조정한 **PC/스위치 공통 확정값**이다. 이후 번역·빌드·검수에서 플랫폼별 예외를 두거나 임의로 원래 값으로 되돌리지 않는다. UI/XML/EBM 권위본은 `translations/romfs/` 아래의 동일 파일을 두 플랫폼이 공유한다. 플랫폼 차이는 설치 형식(PC의 EXE/PAK, 스위치의 NSO/IPS/romfs)뿐이며, 표시 길이·패널 크기·줄 수·자동 줄바꿈 기준은 동일하게 유지한다.

### 용어집

- 대상: `Saves/ui/glossary/uil_glossary_info.xml`.
- 상세 패널 위치는 `X=748`, 패널 폭은 `461`, 본문 줄 배경 폭은 `424`, 본문 `line_char_length=23`을 유지한다.
- 이름 바는 `size_w=434`, `size_wh=395,41`, 이름 텍스트 X는 `180`, 페이지 탭 X는 `448`을 유지한다.
- 용어집 설명은 분류/카테고리 헤더 뒤처럼 구조상 필요한 `<CR>`만 유지한다. 설명문 중간의 불필요한 강제 `<CR>`은 제거하고 23자 자동 줄바꿈을 우선한다. 단어 한가운데를 끊는 `<CR>`은 금지한다.
- 현재 용어집 전체 정리 기준은 설명 234개에서 중간 강제 `<CR>`을 제거한 방식이며, 같은 유형을 새 번역에도 적용한다.
- 게임은 등록된 용어집 표제어를 **단어 경계가 아닌 부분 문자열로 검사**하므로, 번역문에 표제어가 우연히 포함되기만 해도 강조/용어집 링크가 발생할 수 있다. 예: 일반어 `상관없어` 안의 `상관`.
- 용어집 표제어가 대응 일본어 원문에는 없는데 한국어 번역에만 등장하는 경우 `tools/audit_glossary_substring_collisions.py`로 오탐 여부를 확인한다. 일반어/다른 고유명사라면 의미를 유지하는 다른 표현으로 바꾸고, 표제어 자체가 광범위하게 충돌하면 `想観→소칸`처럼 충돌하지 않는 고유 표기로 바꾼다.
- 히라가나/가타카나 표기 차이, `・`, `<CR>` 삽입, 진짜 합성 용어처럼 일본어 쪽에서도 같은 용어를 포함하는 경우는 오탐으로 취급하지 않는다. 번역·용어 변경 후에는 용어집 부분 문자열 감사를 다시 실행한다.

### 미션 / 에피소드 안내

- 대상: `Saves/ui/main_menu/uil_main_menu_top.xml`의 `episode_notice/message`.
- `line_char_length`는 **17**을 사용한다. 기존 15자로 되돌리지 않는다.
- 패널/배경 크기는 현재 값을 유지하고, 텍스트가 넘칠 때는 먼저 17자 자동 줄바꿈을 활용한다.

### 히스토리 / 스토리 개요

- 대상: `Saves/ui/main_menu/uil_main_menu_story_outline.xml`의 본문 `text`.
- `line_char_length`는 **33**을 사용한다. 기존 25자로 되돌리지 않는다.
- 확장된 카드 UI 폭을 유지한다: `flow_prev_line/flow_next_line` X=`299`, `window_bg` 폭=`562`, 외곽 `etc_window02_04` 폭=`599`, `title_bg`/본문 줄 배경 폭=`564`, `highlight` 폭=`604`.
- 위 카드 폭은 실게임 테스트본에서 확인된 값이므로 별도 요청 없이 원래의 좁은 값(`253/474/511/476/516`)으로 되돌리지 않는다.

### 필드 `fm_talk_data`

- 데이터: `translations/romfs/Saves/tweet/fm_talk_data.xml`.
- UI: `Saves/ui/fieldmap/uil_fm_tweet.xml`.
- `line_char_length`는 **22**, 한 메시지는 최대 **3줄(66표시칸)**로 사용한다.
- 기존 20자 기준 강제 `<CR>`이 새 22자 자동 줄바꿈과 겹치면 설치용 결과에서 제거한다. 의미상 문단 구분이 아닌 단순 폭 맞춤용 `<CR>`을 새로 넣지 않는다.

### 이벤트 EBM 대화창

- 이벤트 EBM은 **24자×최대 3줄(72표시칸)** 기준으로 재배치한다.
- 큰 이벤트 창 `Saves/ui/message_window/uil_message_window.xml`은 **패널 크기 `746.667×226.667`과 위치를 절대 늘리지 않고 유지**한다. 본문은 `line_char_length=24`, `limit_width=740`을 사용한다.
- 작은 `Saves/ui/message_window/uil_message_window_normal.xml`도 `line_char_length=24`를 사용한다.
- 로그창 `Saves/ui/message_log/uil_message_log.xml`과 `uil_message_log2.xml`의 본문도 대화창과 동일하게 `line_char_length=24`를 사용한다. 로그 패널 크기·위치는 유지한다.
- 이벤트 메시지 런타임의 **20자 제한은 PC/스위치 모두 24자로 맞춘다.** XML/EBM만 24자로 바꾸고 실행 바이너리의 런타임 제한을 20으로 남겨두는 상태는 허용하지 않는다. PC는 `ArnosurgeDX.exe`에서 해당 이벤트 메시지 루틴의 `20.0→24.0 / 20→24`를 적용한다. 스위치는 `tools/build_main_text_patch.py`/`tools/build_exefs_ui_patch.py`가 1.0.1 NSO의 이벤트 메시지 루틴에 **640→768(20×32→24×32), 20→24** 원시 패치 6개를 `ArNosurgeKoreanUI` IPS에 함께 넣는다. 다른 UI가 공유하는 전역 상수는 건드리지 않고 이벤트 메시지 루틴에만 한정한다.
- EBM 번역 문자열의 `<CR>`, `\r`, `\n`을 먼저 확인한다. 실제 문자열에 개행이 없는데 20자에서 꺾이면 데이터 문제가 아니라 PC 런타임 패치 누락을 의심한다.

### 기타 확정 UI 폭

- 아이템 설명: `Saves/ui/item_info/uil_item_info_page_itemtext.xml` → `line_char_length=18` (기존 15).
- 캐릭터 도감 상세: `Saves/ui/library/uil_character_detail.xml` → `line_char_length=19` (기존 15), 기존 `limit_width=426.66` 유지.
- 몬스터 도감 상세: `Saves/ui/library/uil_monster_detail.xml` → `line_char_length=17` (기존 15), 기존 `limit_width=360` 유지.
- 라이브러리 헬프 상세: `Saves/ui/library/uil_library_help_detail.xml` → `line_char_length=17` (기존 15), 기존 `limit_width=460` 유지.
- 프렌드 스킬 상세 본문: `Saves/ui/library/uil_library_friend_skill_detail.xml` → `line_char_length=17` (기존 15).
- 메인 메뉴 상태/프로필 설명: `Saves/ui/main_menu/uil_main_menu_status.xml` → `line_char_length=19`, 본문 X=`35`를 유지한다 (기존 15 / X=60).
- 송 설명: `Saves/ui/main_menu/uil_main_menu_song.xml` → `line_char_length=17` (기존 15).
- 위 UI들은 이미 확보된 빈 공간을 활용해 텍스트 폭만 늘린 경우가 대부분이므로, **패널 자체 크기는 사용자가 별도로 요청하지 않는 한 늘리지 않는다.**

### UI 변경 검증 원칙

- 줄당 글자 수를 늘릴 때는 `line_char_length`뿐 아니라 `limit_width`, 런타임 하드코딩, 데이터의 강제 `<CR>`이 서로 독립적으로 상한을 만들 수 있음을 항상 확인한다.
- 화면에서 여백이 남는데 줄바꿈이 그대로면 XML 값을 추측해서 반복 변경하지 말고, 실제 설치본 PAK readback → EBM/XML 숨은 개행 확인 → EXE 런타임 덮어쓰기 순서로 원인을 좁힌다.
- 패널 크기·위치·장식 위치는 텍스트 공간이 남아 있는 한 변경하지 않는다. 텍스트 표시 폭/글자 수만 우선 조정한다.
- 테스트 수정은 소스/중간 빌드에만 남기지 않는다. PC는 사용자가 실제 실행하는 `Ar.Nosurge.DX_PC`까지, 스위치는 실제 배포/테스트용 Atmosphère·romfs·exefs 패치까지 같은 값으로 반영한 뒤 readback으로 확인한다.

## 릴리스 패키징 규칙

- PC/Switch 설치기는 **항상 v0.2 Patch ZIP에 들어 있던 검증본을 바이트 단위로 그대로 재사용**한다. 내부 버전 문자열, 백업 폴더명, 문구를 새 버전에 맞춘다는 이유로 수정하지 않는다.
  - PC: `install.bat`, `uninstall.bat`, `install_pc_patch.ps1`, `uninstall_pc_patch.ps1`
  - Switch: `setup_switch.bat`, `build_switch_layout.ps1`
- 새 Patch ZIP을 만든 뒤 위 설치 파일들을 v0.2 Patch ZIP과 SHA-256으로 비교해 전부 동일한지 확인한다.
- 동영상 내용이 바뀌지 않은 릴리스에서는 **새 PC/Switch Movies ZIP을 만들거나 업로드하지 않는다.** 릴리스 본문에는 기존 v0.2 Movies ZIP 링크를 안내한다.
- 동영상 자체가 실제로 변경된 경우에만 새 Movies ZIP을 생성한다. `tools/build_release_packages.py`의 동영상 생성은 명시적 `--include-movies` 옵션을 쓴 경우에만 수행한다.
- GitHub Release 자산을 교체했으면 릴리스 본문의 Patch SHA-256도 실제 업로드된 최신 자산과 다시 맞춘다.

## 작업 순서

- 번역 데이터를 대량 수정하기 전에 기존 규칙과 현재 작업 트리를 확인한다.
- 자동 번역·축약 결과는 위 규칙과 용어집을 통과한 경우에만 반영한다.
- 빌드 산출물이나 캐시를 권위본보다 우선하지 않는다.
- 사용자가 실제 번역 진행을 요청하기 전에는 규칙 정리만 하고 번역 데이터는 변경하지 않는다.
